function reverseString(str) {
    // Split the string into an array, reverse the array, then join it back into a string
    return str.split('').reverse().join('');
  }
  console.log(reverseString("Vikash"));
  